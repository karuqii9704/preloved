import Link from 'next/link'
export function AdminList({title,description,newHref}:{title:string;description:string;newHref?:string}){return <main className="shell section"><p className="eyebrow">Admin</p><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:12}}><div><h1 style={{margin:'4px 0'}}>{title}</h1><p style={{color:'var(--muted)'}}>{description}</p></div>{newHref&&<Link href={newHref} className="btn">Tambah</Link>}</div><div className="card" style={{padding:24,marginTop:24}}><p style={{margin:0}}>Belum ada data. Hubungkan Supabase lalu kelola data dari halaman ini.</p></div></main>}
