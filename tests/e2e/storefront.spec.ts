import { test, expect } from '@playwright/test'
test('buyer can discover a product and add it to cart @smoke',async({page})=>{await page.goto('/');await expect(page.getByRole('heading',{name:/Preloved yang terasa seperti toko/i})).toBeVisible();await page.getByRole('link',{name:/Lihat storefront/i}).click();await page.getByRole('link',{name:/Cardigan Rajut Biru/i}).click();await page.getByRole('button',{name:'Tambah ke keranjang'}).click();await page.getByRole('link',{name:/Keranjang, 1 produk/i}).click();await expect(page.getByText('PLV-0001')).toBeVisible();await expect(page.getByRole('button',{name:'Lanjutkan pesanan'})).toBeVisible()})
