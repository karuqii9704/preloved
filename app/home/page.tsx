import Link from 'next/link'
import Image from 'next/image'
import { ArrowRight } from 'lucide-react'
import { StoreShell } from '@/components/store-shell'
import { ProductCard } from '@/components/product-card'
import { activeProducts } from '@/lib/catalog'
export default function StoreHome(){const featured=activeProducts.filter(p=>p.featured);return <StoreShell><section className="shell" style={{padding:'1rem 0 2.5rem'}}><div style={{display:'grid',gridTemplateColumns:'minmax(0,1fr)',background:'var(--blue-soft)',borderRadius:16,overflow:'hidden',minHeight:360}}><div style={{padding:'2.5rem 1.5rem',display:'flex',flexDirection:'column',justifyContent:'center'}}><p className="eyebrow">Koleksi terbaru</p><h1 style={{fontSize:'clamp(2.3rem,7vw,4.8rem)',lineHeight:.96,letterSpacing:'-.07em',margin:'8px 0 18px'}}>Temukan barang yang masih layak dipakai.</h1><p style={{maxWidth:420,color:'var(--muted)',margin:'0 0 24px'}}>Lihat kondisi, harga, dan detail barang sebelum memutuskan untuk memesan.</p><Link href="/shop" className="btn" style={{width:'fit-content'}}>Jelajahi katalog <ArrowRight size={17}/></Link></div><div style={{position:'relative',minHeight:280}}><Image src="https://images.unsplash.com/photo-1558618666-fcd25c85cd64?auto=format&fit=crop&w=1200&q=85" alt="Koleksi pakaian preloved" fill priority sizes="100vw" style={{objectFit:'cover'}}/></div></div></section><section className="shell section"><div style={{display:'flex',justifyContent:'space-between',alignItems:'end',marginBottom:18}}><div><p className="eyebrow">Pilihan utama</p><h2 style={{margin:0,fontSize:'clamp(1.7rem,4vw,2.4rem)',letterSpacing:'-.04em'}}>Barang unggulan</h2></div><Link href="/highlighted" style={{fontWeight:700,fontSize:'.9rem'}}>Lihat semua</Link></div><div className="grid">{featured.map(product=><ProductCard key={product.id} product={product}/>)}</div></section><section className="shell section"><div style={{background:'var(--pink-soft)',padding:'1.5rem',borderRadius:16}}><p className="eyebrow">Cara memesan</p><div style={{display:'grid',gap:20,gridTemplateColumns:'repeat(3, minmax(0,1fr))'}}>{['Pilih barang','Isi nama dan alamat','Lanjut ke WhatsApp'].map((item,i)=><div key={item}><strong style={{fontSize:'1.2rem'}}>{String(i+1).padStart(2,'0')}</strong><p style={{margin:'6px 0 0'}}>{item}</p></div>)}</div></div></section></StoreShell>}
