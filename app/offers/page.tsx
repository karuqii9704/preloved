import { StoreShell } from '@/components/store-shell'; import { ProductCard } from '@/components/product-card'; import { activeProducts } from '@/lib/catalog'
export default function Offers(){const offers=activeProducts.filter(p=>p.promoPrice);return <StoreShell><section className="shell section"><p className="eyebrow">Harga khusus</p><h1 style={{letterSpacing:'-.05em'}}>Offers</h1>{offers.length?<div className="grid">{offers.map(p=><ProductCard key={p.id} product={p}/>)}</div>:<p>Belum ada penawaran aktif.</p>}</section></StoreShell>}
